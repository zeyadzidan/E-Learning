import { ToggleButton } from "@mui/material";

export default function WishlistButton() {
    return (
        <ToggleButton
            value="check"
        >
            WishlistButton
        </ToggleButton>
    )
}